# Migration `20190905092909`

This migration has been generated by maxime blanc at 9/5/2019, 9:29:09 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new__CategoryToPost"("A" TEXT    REFERENCES "Category"(id) ON DELETE CASCADE,"B" TEXT    REFERENCES "Post"(id) ON DELETE CASCADE)
;

INSERT INTO "new__CategoryToPost" ("A","B") SELECT "A","B" from "_CategoryToPost"

DROP TABLE "lift"."_CategoryToPost";

ALTER TABLE "lift"."new__CategoryToPost" RENAME TO "_CategoryToPost";

CREATE UNIQUE INDEX "lift"."_CategoryToPost_AB_unique" ON "_CategoryToPost"("A","B")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190905092909
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,32 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+model User {
+  id    String  @default(cuid()) @id
+  email String  @unique
+  name  String?
+  posts Post[]
+}
+
+model Post {
+  id         String     @default(cuid()) @id
+  createdAt  DateTime   @default(now())
+  updatedAt  DateTime   @updatedAt
+  published  Boolean
+  title      String
+  content    String?
+  author     User?
+  categories Category[]
+}
+
+model Category {
+  id    String @id @default(cuid())
+  name  String
+  posts Post[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190905092909)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190905092909'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
